# Vite でクライアントに公開される環境変数（VITE_ で始まるもの）
# 開発用のデフォルト値

# src/main.jsx で使用するリモートカタログ JSON
VITE_REMOTE=https://raw.githubusercontent.com/Neosku/aviutl2-catalog-data/main/index.json

## 開発専用のプロキシターゲット
VITE_API_PROXY_TARGET=https://post-issues.neosku.workers.dev

# Submit ページ（multipart/form-data）の送信先
VITE_ISSUES_ENDPOINT=/api/issues

# プレフィックスを削除しない場合は空のまま（例: '/api/issues' -> '/api/issues' のまま）
VITE_API_PROXY_STRIP_PREFIX=