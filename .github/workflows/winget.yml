name: Publish to WinGet
on:
  release:
    types: [released]
  workflow_dispatch:  
jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: Neosku.AviUtl2-Catalog
          installers-regex: '\.exe$' # Only .exe files
          token: ${{ secrets.WINGET_TOKEN }}
